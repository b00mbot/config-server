server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/b00mbot/configurations-test.git
          search-paths: '{application}'
management:
  security:
    enabled: false
